using System;

namespace STELLAR.APP
{
    public enum ProcessStatusEnum
    {
        Running,
        Paused,
        Completed,
        Error,
        Disabled,
        Waiting,
        Stopped
    }
}
